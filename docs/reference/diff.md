# Diffing

Compare two apks (or bundles — `diff()` is duck-typed against `ApkFile`'s attribute names) via
[`ApkFile.diff`][apkfile.ApkFile.diff] or the standalone function below.

::: apkfile.diff

::: apkfile.ApkDiff
