# Signing

Signing scheme detection and certificate metadata, available via [`ApkFile.signing`][apkfile.ApkFile.signing]
(and the equivalent property on every bundle class, which delegates to the base apk).

::: apkfile.SigningInfo

::: apkfile.Certificate

::: apkfile.SigningScheme
